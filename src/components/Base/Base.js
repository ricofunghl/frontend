import React, {Component} from 'react';
import AppBar from '../AppBar/AppBar';

class Base extends Component {
    render () {
        return (
            <AppBar />
        )
    }
}

export default Base;